apiVersion: v1
kind: ConfigMap
metadata:
  name: dapr-config
  namespace: dapr-system
data:
  config.yaml: |
    apiVersion: dapr.io/v1alpha1
    kind: Configuration
    metadata:
      name: dapr-config
      namespace: dapr-system
    spec:
      mtls:
        enabled: false